[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "enterprise-implementation-lab"
version = "0.1.0"
description = "Synthetic requirements-to-rollout reference implementation"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Oğuzhan Özdemir" }]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
